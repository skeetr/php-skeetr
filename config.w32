// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("skeetr", "for skeetr support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("skeetr", "enable skeetr support", "no");

if (PHP_ECHO != "no") {
	EXTENSION("skeetr", "skeetr.c");
}

